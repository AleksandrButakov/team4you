# create a separate config WebDriverConfig, which reads this file
webdriver.baseUrl=https://www.rshb.ru

# create a separate config BrowserConfig for local tests, which reads this file
browser.local.type=chrome
browser.local.version=117.0
browser.local.size=1920x1080

# create a separate config BrowserConfig for remote tests, which reads this file
browser.remote.type=firefox
browser.remote.version=112.0
browser.remote.size=1920x1080

# create a separate config RemoteConfig, which reads this file
isRemote.driver=true
remote.driver=http://62.113.108.218:4444/wd/hub
remote.threads.driver=null

# create a separate config SelenoidConfig, which reads this file
selenoid.name=My tests https://rshb.ru
selenoid.sessionTimeout=15m
selenoid.env=TZ=UTC
selenoid.console=true
selenoid.enableVNC=true
selenoid.enableVideo=true